{
  "layer": "top",
  "position": "top",
  "height": 32,
  "spacing": 8,

  "modules-left": [
    "custom/launcher",
    "tray"
  ],

  "modules-center": [
    "hyprland/workspaces"
  ],

  "modules-right": [
    "custom/language",
    "network",
    "pulseaudio",
    "clock",
    "custom/power"
  ],

  // ─────────────────────────────────────────────────────────────
  // LEFT
  // ─────────────────────────────────────────────────────────────

  "custom/launcher": {
    "format": "",
    "tooltip": true,
    "tooltip-format": "Launcher (stub)",
    "on-click": "~/.config/waybar/scripts/launcher.sh"
  },

  "tray": {
    "spacing": 10
  },

  // ─────────────────────────────────────────────────────────────
  // CENTER
  // ─────────────────────────────────────────────────────────────

  "hyprland/workspaces": {
    "format": "{name}",
    "on-click": "activate",
    "sort-by-number": true,
    "persistent-workspaces": {
      "1": ["HDMI-A-1"],
      "2": ["HDMI-A-1"],
      "3": ["HDMI-A-1"],
      "4": ["HDMI-A-2"],
      "5": ["HDMI-A-2"],
      "6": ["HDMI-A-2"]
    }
  },

  // ─────────────────────────────────────────────────────────────
  // RIGHT
  // ─────────────────────────────────────────────────────────────

  "custom/language": {
    "format": "  {}",
    "tooltip": true,
    "tooltip-format": "Keyboard layout",
    "exec": "~/.config/waybar/scripts/language.sh",
    "interval": 1,
    "on-click": "~/.config/waybar/scripts/language.sh --next"
  },

  "network": {
    "format-wifi": "  {signalStrength}%",
    "format-ethernet": "󰈀  wired",
    "format-disconnected": "  no net",
    "tooltip": true,
    "tooltip-format-wifi": "{essid} ({signalStrength}%)\n{ipaddr}",
    "tooltip-format-ethernet": "{ifname}\n{ipaddr}",
    "tooltip-format-disconnected": "Disconnected",
    "on-click": "kitty --class floating-tool -T Network -e nmtui"
  },

  "pulseaudio": {
    "format": "  {volume}%",
    "format-muted": "  muted",
    "scroll-step": 5,
    "on-click": "pavucontrol",
    "on-click-right": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"
  },

  "clock": {
    "format": "  {:%H:%M}",
    "format-alt": "  {:%a, %d %b}",
    "tooltip": true,
    "tooltip-format": "<tt>{calendar}</tt>",
    "on-click": "~/.config/waybar/scripts/calendar.sh"
  },

  "custom/power": {
    "format": "⏻",
    "tooltip": true,
    "tooltip-format": "Power menu",
    "on-click": "~/.config/waybar/scripts/power.sh"
  }
}

